Реализация поиска в каждом диске в отдельном потоке(исключил диск C,тк времязатратно), 
поиск в каждой папке корневой директории в отдельном потоке(для быстроты использовал диск B:, но можно указывать и свои корневые директории).
Потокобезопасность Singleton'а реализовывал через Lazy<T>
